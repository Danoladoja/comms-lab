{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "migrate:certificate-codes": "tsx ./src/migrate-certificate-codes.ts",
    "seed:rubrics": "tsx ./src/seed-default-rubrics.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "credit:attendance": "tsx ./src/credit-missed-attendance.ts",
    "check:schema": "tsx ./src/check-schema.ts",
    "why:locked": "tsx ./src/why-locked.ts",
    "freeze:critiques": "tsx ./src/freeze-critique-requirements.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@workspace/db": "workspace:*",
    "@workspace/domain": "workspace:*",
    "drizzle-orm": "catalog:",
    "tsx": "catalog:"
  }
}
