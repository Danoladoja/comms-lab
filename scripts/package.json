{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "migrate:certificate-codes": "tsx ./src/migrate-certificate-codes.ts",
    "seed:rubrics": "tsx ./src/seed-default-rubrics.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@workspace/db": "workspace:*",
    "@workspace/domain": "workspace:*",
    "drizzle-orm": "catalog:",
    "tsx": "catalog:"
  }
}
